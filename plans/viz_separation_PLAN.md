For some context: currently to compile i would need to bring in both arrow and rerun, this is not possible in the locked down system I am transfering the repository to. As such, I would want an even stronger separation between the running of online semantic and the visualizer.

For the refactor, i want the visualizer to be in python, this would handle the rerun logging and live viewer. Note that this live viewer is not currently present in the current implementation and you will need to think about how to extend it to live viewing. Shouldn't be too difficult as the hooks already provide all the information that is required. You can now remove the requirement of rerun and arrow from the CMakeLists, as well as the target viz_semantic_universe. We will now only use run_semantic_universe, this should no longer have the gmd logic, we will push down the road (I want a function that can do everything and output the gmd stuff)

Now for the communication between the C++ run_semantic_universe to the python visualizer utilities, we will again rely on IPC with msgpack? Ideally we send the objects and all the constituent points everytime. Don't do the delta tracking for now. You are free to decide on a message format so long as the visualizer gives out an output that is the same to the modern day viz_semantic_universe. For the live rerun viewing, you should just be able to log everything everytime? it shouldn't take up a lot of memory as it should just overwrite. For the .rrd file, do the same thing where some stuff are logged per frame and some others are logged only with finish().
