# Build artifacts
/build/
/build-cuda/
/CMakeFiles/
/CMakeCache.txt
compile_commands.json

# Vendored deps (each is its own git repo)
/cppzmq/
/libzmq/
/hdbscan/
# inf_server & mask3d_feat: our wrapper code is committed; the big upstream trees
# (DVIS_Plus, Mask3D, MinkowskiEngine) are git submodules (see .gitmodules).

# Python venvs, caches, and model weights (IPC servers) — never in git
**/.venv/
**/__pycache__/
*.py[oc]
*.pth
*.ckpt

# Large data / recordings / outputs
*.rrd
/recordings/
*.deb

# Harness
/.claude/

# ONNX export sidequest (standalone, not part of the live pipeline)
/dvis_export/
EOF
