# Interface Types for RA's C++ Modules

This is a subset of RA's C++ message definitions and common structs that might be useful.

Sample usage can be found in `main.cpp`, with a toy `CMakeLists.txt` also provided.
